---
title: "Math"
---

## add

```ts
add(2, 3); // => 5
```

単純に足し算をする関数です。  
わざわざ関数にするほどでもないので僕は使いません。  
カリー化されているので、そこが便利といえば便利かも。

## dec

number を引数に取り、1 減らした number を返す関数です。

```ts
dec(40); // => 39
```
